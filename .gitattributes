# Sem isto, um clone em Windows pode gravar o install.sh com CRLF, e aí o Linux
# responde "bad interpreter: /bin/sh^M". Os fins de linha ficam decididos aqui,
# e não pela configuração de quem clona.

* text=auto

*.sh   text eol=lf
*.yml  text eol=lf
*.ps1  text eol=crlf
*.ico  binary
